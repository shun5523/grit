<div class="nested-fields">
	<%= f.label "国名" %>
	<%= f.collection_check_boxes :country_id, Country.all,:id,:country_name %>

	<%#= f.collection_select(:country_id, Country.all, :id, :country_name) %>
	<%#= link_to_remove_association '削除', f %>
</div>
